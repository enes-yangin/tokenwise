# Görev: Kod tabanı anlama (bağlamsal)

Kaynak (SADECE OKU, değiştirme): `C:/Users/Enes/Desktop/skills/tokenwise/`
Aşağıdaki 6 soruyu, kaynağı inceleyerek net ve kısa yanıtla. Cevapları `ANSWERS.md`
dosyasına, `1) ... 2) ...` biçiminde yaz. Her cevap için dayandığın dosyayı belirt.

1. Kullanıcı `/tokenwise off` yaptığında flag dosyasına hangi değer YAZILIR
   (silinmek yerine)? Hangi dosyada?
2. Otomatik yüklenen `RULES.md` için karakter sınırı kaçtır ve hangi dosyada
   uygulanır?
3. `readMode()` içinde flag dosyası ile `TOKENWISE_MODE` env değişkeni çakışırsa
   hangisi kazanır?
4. Flag dosyası nasıl "proje bazlı" yapılıyor — hangi modül, hangi mekanizma
   (nasıl bir anahtar üretiliyor)?
5. Plugin hangi iki hook olayına (event) kayıt oluyor?
6. `full` modda ruleset'e eklenen ama `lite` modda olmayan paragraf hangisidir?

Sadece kendi dizininde (ANSWERS.md yazımı için) çalış; kaynağı değiştirme.
