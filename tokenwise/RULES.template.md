# RULES.md

Drop this file at your project root and fill it in. tokenwise auto-loads it at the
start of every session, so you never spend tokens re-explaining your conventions and
Claude stops drifting from your project's style. Keep it short — only what isn't
obvious from the code itself. Delete the prompts below as you answer them.

## Stack & tooling
<!-- e.g. Python 3.12, FastAPI, pytest, ruff. Test: `pytest`. Lint: `ruff check`. -->

## Conventions Claude keeps getting wrong
<!-- The specific things you find yourself correcting every session.
     e.g. "Use pathlib, not os.path." / "Type-hint every public function."
     "Raise domain exceptions, don't return None on failure." -->

## Architecture / layout
<!-- Where things live and the boundaries that matter.
     e.g. "Business logic in services/, never in routers/." -->

## Definition of done
<!-- e.g. "Every change has a test. Run `pytest && ruff check` before claiming done." -->

## Do NOT
<!-- Hard nos. e.g. "Never add a dependency without asking." "Don't edit migrations/." -->
