[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "expense-tracker"
version = "0.1.0"
description = "Simple expense tracking library"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["."]
